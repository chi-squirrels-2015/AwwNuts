<div class="commments">
  <% comments.each do |comment| %>
    <div class="comment-content">
      <p><%= comment.content %> - <strong><%= comment.author.username %></strong> <span class="text-muted"><%= time_ago_in_words(comment.created_at) %> ago</span></p>
    </div>
  <% end %>
</div>	
